# 着地見込みのYouTubeデータ鮮度差対応（deep-think 最終レポート）

日付: 2026-07-18／参加: Claude（提案）× Codex（反論）× auditor（判定）

## 問い・制約・判断基準

**問い**：プラットフォームごとにデータ確定ラグが異なる（DLsite 1日／Fanza 2日／YouTube 3日）状況で、月次着地見込みをどう計算・表示すれば妥当性を担保できるか。

**制約**：既存 Next.js + Supabase 内で完結／保守コスト小（複雑な統計モデルは持たない）／確定ラグ自体は変えられない（YouTube Analytics API側の仕様）／定額運用。

**判断基準**：(1)鮮度差による系統バイアスの除去（全体・PF別・ブランド別すべて） (2)実績と推定の境界の透明性 (3)異常時に黙って外挿しない (4)実装のシンプルさ。

## 前提事実（調査で確定）

- 直近YouTubeが取れていないのは**ジョブ障害ではなく、YouTube Analytics APIのデータ確定ラグ（約3日）**。取り込みジョブは毎日成功しており、直近7日を再取得するローリング方式で遅れた分は数日後に自動で埋まる。過去90日に日付の穴はない。
- 現行の見込みは「何かしらデータがある日の直近3日の合算平均×残日数」。このため直近1〜2日はYouTube分（売上構成比 約13.7%）やFanza分がゼロのまま平均に入り、**着地見込みが恒常的に過小**に出る。ユーザーの「妥当性が無い」という体感は正しい。
- プラットフォーム別見込みはさらに深刻で、YouTubeの「直近3日平均」は3日中1日しかデータがなく実態の約1/3になる。
- 見込みの実装経路は3箇所ある：`src/lib/queries/dashboard.ts:143`（KPI）／`dashboard.ts:459`（チャート）／`src/lib/queries/monthly-report.ts:290`。

## 結論

**「プラットフォーム別の確定日 C_p を基準に、最小セル（PF×ブランド×言語）単位で暦日ゼロ埋めして計算し、全表示をそのセルの合算から導出する」方式を採用する。**

1. **確定日 C_p**：予測が読むのと同じ集計ソース（daily_breakdown_summary）から導出。DLsite/Fanza = MAX(sale_date)。YouTubeは片チャンネル欠損対策として platform='youtube' × language 別 MAX(sale_date) の最小値（auditor指摘反映：生テーブルでなくMVから取ることで自己整合を保つ）。
2. **計算**：セルごとに `rate = [C_p−2, C_p] の3暦日合計÷3`（不存在日は確定ゼロ扱い）、`actual = 月初〜C_p`、`tail = rate × (C_p翌日〜月末の実日付差)`。全体・PF別・ブランド別・言語別はすべてセルの和 → **定義上、全表示が一致**。unknownブランド/言語もセルに含める。
3. **出力契約**：KPIには総額（actual+tail）、チャートには将来分（tail）のみを渡す（実績の二重計上防止）。3経路を共通関数 `computeForecast()` に集約。
4. **月初対策**：前月ルックバック取得に platform/brand/language 列を追加し、C_pが前月でも破綻しない実日付差計算にする。
5. **比較指標も揃える**：前月比・前年比は、PFごとに C_p と同じ月内日数までの過去実績と比較（as-of揃え）。
6. **異常時ガード（PF別SLA）**：遅れ > 恒常ラグ+2日で警告バッジ、+5日で見込みを「—（算出不可）」に降格。実績と鮮度表示は出し続ける。
7. **透明性**：見込みの近くに「反映済み: DLsite 7/17 / Fanza 7/16 / YouTube 7/15」の鮮度表示。
8. **予測窓の最終決定はバックテストで**：実装前に過去3〜6ヶ月をas-of巻き戻しで再現するスクリプトを書き、①現行方式 ②共通最終日打ち切り（ユーザー発案の案A） ③PF別窓 ④共通暦日窓＋PF別実績 の4方式を符号付き誤差・MAE・日次変動幅で比較して勝者を採用（③④が拮抗なら曜日構成が揃う④）。バックテストには「過去の暫定値は再現できない」近似前提を明記する。

**Phase 2（別PR・バックログ）**：`platform_data_freshness` ウォーターマークテーブル（取込側が完全性日付を記録）／DLsite日次スクレイプの3日窓化（後日訂正の回収）／MV更新失敗の可視化。

## 根拠と却下した代替案

- **案A：全PFが揃う最終日で打ち切り（ユーザー発案）** — ゼロ埋め混入は防げるが、売上の約79%を占めるDLsiteの確定済み直近2日を捨て、外挿区間が延びる。ただし「上位互換」と断定はせず、バックテストの比較対象として残した（②）。
- **案C：欠損日を推定値で補完してから現行ロジック** — 実績と推定の境界が曖昧になり判断基準(2)に反する。不採用。
- **案D：曜日調整等の高度な予測モデル** — 本件の本質はモデル精度でなく入力の系統バイアス。保守コストに見合わず不採用。
- **ウォーターマークテーブルの即時導入** — 正攻法だが取込3経路の変更＋migrationが同時に必要で変更範囲が過大。Phase 2に分離（auditorもこの分離を妥当と判定）。

## 議論の経緯

- **ラウンド1（Claude初案）**：PFごとに「自分のデータがある直近3日」で個別計算して合算する案B。
- **Codex反論（重大14件・中7件）**：①最終売上日≠確定日（不存在＝未確定かゼロか区別不能。疎セルでは「売上発生日平均」化して過大予測に転じる） ②全体と内訳の合算不一致 ③実装経路は3箇所（KPI見落とし） ④チャートは将来分のみの契約で総額を渡すと実績二重計上 ⑤月初処理未定義 ⑥前月比バイアス放置 ⑦YouTube片チャンネル欠損 ⑧Actions成功≠MV更新成功 ⑨警告だけで外挿継続は不十分 ⑩「案Aの上位互換」未証明——など。代替案として「PF確定日をセルに共通適用＋暦日ゼロ埋め＋最小セル合算」を提示。
- **ラウンド2（Claude修正）**：Codexの代替案を骨格に全面改訂（上記結論）。ほぼ全指摘を受け入れ、重量級のウォーターマーク化はPhase 2に分離。
- **auditor判定**：**合格**。実コード照合（dashboard.ts:143-157、MonthlyForecastChart.tsx:13-43、011_dashboard_speedup.sql:29-39、youtube-ingest.ts:46）でCodex指摘の事実とv2対応の正しさを両方確認。軽微な注意3点（C_youtubeはMVから導出へ統一／バックテストの近似前提明記／片チャンネル停止時の表示エッジ）は本レポートに反映済み。

## 未解決論点（要・加藤さん判断）

- **技術的な未解決論点はなし**（auditor合格）。
- 判断が必要なのは進め方のみ：
  1. **実装に進めるか**（進める場合は codex-implement で発注。作業順：バックテスト→窓方式決定→computeForecast実装→3経路置き換え→UI）
  2. **Phase 2（ウォーターマーク・DLsite 3日窓化）をいつやるか**（急ぎではない。Phase 1で実害は解消される）

## 判定結果

auditor（fable・議論不参加）判定：**合格**。Codex反論1回→修正1回の計1往復で決着（上限3往復以内）。
中間ファイル：セッションscratchpad/deep-think/（round1_proposal.md, round1_codex.md, round2_proposal.md）
